apiVersion: apps/v1
kind: Deployment
metadata:
  name: quizzes-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quizzes-mongo
  template:
    metadata:
      labels:
        app: quizzes-mongo
    spec:
      containers:
        - name: quizzes-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: quizzes-mongo-srv
spec:
  selector:
    app: quizzes-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017